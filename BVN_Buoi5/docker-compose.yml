version: "3.9"

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: pagination
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"

  backend:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - db
